namespace JRogue.Status
{
    /// <summary>Whether a status helps or hinders the bearer (rest gates, future AI, etc.).</summary>
    public enum StatusPolarity
    {
        Neutral = 0,
        Negative = 1,
        Positive = 2
    }
}
